{
  // Cloudflare Workers「純靜態資產」部署設定（無 Worker 程式,只送 public/ 裡的網頁+訊號）。
  // ⚠ directory 指到 ./public 子資料夾（**不是 repo 根**）→ 避免把 .git/、README、設定檔當資產公開。
  "name": "tw-market-dashboard",
  "compatibility_date": "2026-07-01",
  "assets": {
    "directory": "./public"
  }
}
